node_modules/
dist/
out/
__pycache__/
*.pyc
*.npy
*.bin

# The reference turnaround is (c) Riot Games, modelled and textured by Thibaut
# Granet. It is a measurement target and is not redistributed -- nor is anything
# derived from it pixel-wise: the extracted PBR maps, their served copies, the
# de-lit albedo, or any render that displays them. Published imagery is
# geometry-only; see images/PROVENANCE.md.
ref/*.jpg
ref/*.png
ref/views/
pbr/
public/
delit/
